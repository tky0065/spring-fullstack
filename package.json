{
  "name": "@spring-fullstack/cli",
  "version": "1.0.0",
  "description": "CLI pour générer des projets Spring Boot + Frontend",
  "bin": {
    "spring-fullstack": "./bin/cli.js"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "inquirer": "^9.0.0",
    "chalk": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0"
  }
}
